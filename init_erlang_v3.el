;; Erlang/OTP with GTE adaptations
(setq load-path (cons "/proj/ltegte/app/gte-dev-env/emacs/elisp"
		      load-path))
(require 'gte)
     ;; optionally add this, see info on EDTS below: (require 'gte-edts)
(require 'gte-edts)